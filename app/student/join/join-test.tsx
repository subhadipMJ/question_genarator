"use client";import { useState,useSyncExternalStore } from "react";import { useRouter } from "next/navigation";import { toast } from "sonner";import { Button } from "@/components/ui/button";
const subscribe=(callback:()=>void)=>{window.addEventListener("hashchange",callback);return()=>window.removeEventListener("hashchange",callback)};const getHash=()=>window.location.hash;const getServerHash=()=>"";
export default function JoinTest(){const hash=useSyncExternalStore(subscribe,getHash,getServerHash);const token=new URLSearchParams(hash.slice(1)).get("token")??"";const [busy,setBusy]=useState(false);const router=useRouter();async function start(){if(!token)return;setBusy(true);try{const r=await fetch("/api/backend/student/test-series/start",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({invite_token:token})});const d=await r.json();if(!r.ok)throw new Error(d.detail??"Unable to join test.");history.replaceState(null,"",location.pathname);router.push(`/student/attempts/${d.id}`)}catch(x){toast.error(x instanceof Error?x.message:"Unable to join test.")}finally{setBusy(false)}}return <div className="rounded-xl border p-6 text-center"><h1 className="text-2xl font-bold">Join invited test</h1><p className="text-muted-foreground my-4">Confirm when you are ready. The timer starts after you press the button.</p><Button disabled={!token||busy} onClick={start}>{busy?"Starting...":"Start test"}</Button>{!token&&<p className="text-destructive mt-3">This invite link is missing its token.</p>}</div>}
